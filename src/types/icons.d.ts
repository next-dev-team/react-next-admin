//@ts-nocheck

/**  --- OPTIONAL ---
 * auto import will generate type only when we save so when we run dev it will remove those type.
 * to avoid eslint mark it as error we need to Manual icon type here
 */

export {}

declare global {
  const IconEtTools2: typeof SvgComponent
  const IconEmojioneFlagForCambodia: typeof SvgComponent
  const IconTeenyicons360Solid: typeof SvgComponent
  const IconIonRefreshCircleSharp: typeof SvgComponent
}
